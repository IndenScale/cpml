# CPML release configuration for cargo-release
# Usage: cargo release [patch|minor|major]

sign-commit = false
sign-tag = false
push-remote = "origin"
pre-release-commit-message = "chore: release {{version}}"
tag-message = "{{version}}"
tag-name = "v{{version}}"
dev-version = true
publish = false
